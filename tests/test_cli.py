import os
import app
import requests
import unittest

from flask import Flask, request, jsonify, Response

class CLIUnittest(unittest.TestCase):
    """Unit test suite to test the functionality of the `fleet` command line
    options."""

    def setUp(self):
        """Sets up objects before every test"""

        pass

if __name__ == '__main__':
    unittest.main()
